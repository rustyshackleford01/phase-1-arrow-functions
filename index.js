const divide = function(a, b) {
     return a / b;
    };

    // Arrow function for squaring a number
const square = x => x * x;

// Arrow function for adding two numbers
const add = (a, b) => a + b;